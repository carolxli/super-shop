import React from 'react';
import FormCliente from '../components/FormCliente';

const Cliente = () => {
    return (
        <div>
            <FormCliente />
        </div>
    );
};

export default Cliente;